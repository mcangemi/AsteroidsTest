using UnityEngine;

public class loop : MonoBehaviour
{

    // if anything collides with left tag move right 24
    // if anything collides with up tag move down 10
    // if anything collides with right tag move left 24
    // if anything collides with down tag move up 10

    void Update()
    {
        
    }
}
